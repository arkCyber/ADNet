[package]
name = "a3net-gateway"
description = "IPFS-compatible HTTP Gateway for A3Net"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
# Core dependencies
a3net-blobstore = { workspace = true }
a3net-types = { workspace = true }
a3net-observability = { workspace = true }
a3net-namespace = { workspace = true, features = ["dht"] }
a3net-ipc = { workspace = true }
a3net-dht = { workspace = true }

# Crypto
ed25519-dalek = { workspace = true }
blake3 = { workspace = true }
rand = { workspace = true }

async-trait = { workspace = true }

# Async runtime
tokio = { workspace = true, features = ["full"] }
futures = { workspace = true }

# HTTP server
axum = "0.7"
hyper = { version = "1.4", features = ["full"] }
hyper-util = { version = "0.1", features = ["full"] }
http-body-util = "0.1"
http = "1.1"

# WebSocket
tokio-tungstenite = "0.24"
tungstenite = "0.24"
futures-util = "0.3"

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
serde_cbor = "0.11"

# Error handling
thiserror = { workspace = true }
anyhow = { workspace = true }

# Logging
tracing = { workspace = true }

# Utilities
bytes = "1.8"
parking_lot = "0.12"
base64 = "0.22"

[dev-dependencies]
tempfile = { workspace = true }
tokio-test = "0.4"
serde_json = { workspace = true }
a3net-dht = { workspace = true }

[features]
default = []
# Enable the `/metrics` + `/health` HTTP server via a3net-observability.
metrics-http = ["a3net-observability/http-server"]
